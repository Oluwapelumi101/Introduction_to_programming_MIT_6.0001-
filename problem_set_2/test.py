a = "t_ _ m"
b = "taat"

# Python3 code to remove whitespace
def remove(string):
    return "".join(string.split())
     
# Driver Program
string = a
print(remove(string))



# Junk code

          # if (is_letter_guessed(i, other_word, no_space) == False) or (no_space.count(i) != other_word.count(i)) or (my_word_list[0] != other_word_list[0]):
          #   print(i)
          #   neg.append(False)
          #   # return(False)
        # print(i)
        # my_word_list.pop(0)
        # other_word_list.pop(0)
        # print(my_word_list, other_word_list)

          # elif (is_letter_guessed(i, other_word, no_space) == True) and (no_space.count(i) == other_word.count(i)) and (no_space.index(i)) == other_word.index(i):
          #   # print('True')
          #   # return(True)
    # else:
    #    neg.append(False)

    # sending the return
    # print(neg)
    # if False in neg:
    #   return(False)
    # else:
    #   return(True)


      # return(False)
    # my_word_list = []
    # count = 0
    # for i in my_word:
    #   if i != " ":
    #     count += 1
    #     if i != "_":
    #       my_word_list.append(i)
    # # compare length
    # if count == len(other_word):
    #   for i in my_word_list:
    #       if (is_letter_guessed(i, other_word, my_word_list) == False) or (my_word_list.count(i) != other_word.count(i)):
    #         return(False)
    #   return(True)
    # else:
    #   return(False)
