
    # dis_travelled = 0
    # dis_outdoor = 0
    # for obj in w_path[1::]:
    #     print(type(obj)
    
        # if obj != None:
        #     dis_travelled = dis_travelled + edge.get_total_distance()
        #     dis_outdoor = dis_outdoor + edge.get_outdoor_distance()
    # print(dis_travelled, dis_outdoor)


    # for place in path
    # main_path = [[], dis_travelled, dis_outdoor ]

    # calculating distance and distance outdoor:

            # dis_travelled = edge.get_total_distance()
        # dis_outdoor = edge.get_outdoor_distance()
        # print(dis_travelled, dis_outdoor)


                 # print(best_path[1])
                    # len of shortest path
                # for x in best_path[1]:
                #         best_dist = dis_travelled + edge.get_total_distance()
                #         # dis_outdoor = dis_outdoor + edge.get_outdoor_distance()
